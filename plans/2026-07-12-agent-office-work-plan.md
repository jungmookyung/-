# Agent Office 업무 기획 — 2026-07-12 (토)

스크린샷 3장(Agent Office 대시보드, 디스코드 메인봇 대화 2건)을 근거로 현재 상황을 추론하고,
오너(승민)가 직접 처리해야 할 업무와 봇이 진행 중인 작업의 검수 포인트를 기획한 문서.

---

## 1. 상황 추론

### 시스템 구성 (대시보드 기준)
- **Agent Office**: 메인PM 봇(디스코드 "system engineer") + Master Staff + SubPM 4명(1bav34, njzbjb, 8mhj66, j20iyr) + 워커들이 도는 멀티에이전트 오케스트레이션 환경. 픽셀 사무실 UI로 시각화.
- **관측 지표**: 오늘 비용 $1,085.20 / 호출 2,902회 / 출력 2.4M tok. 쿼터 바 — Claude 5시간 42%, Codex 5시간 79%, Claude 주간 39%(7/15 23:00 리셋), Codex 주간 12%(7/18 15:02 리셋).
- **진행 중 태스크보드**: 오너 포트폴리오 사이트(응대 에이전트 위젯 내장) — P1 콘텐츠 팩·P2 디자인 시스템은 완료 후 **확정대기**, P3 사이트 구현은 대기. 담당 SubPM j20iyr, 마감 오늘.
- **보조 시스템**: `cbot-quota`(정기 쿼터 리포트), `quota-alert`(⚡소모 알림), 와처(워커 화면 15분 주기 감시, 정체 시 알림).

### 현재 굴러가는 사안 3건
1. **주간 쿼터 권고 페이스 게이지** (오후 6:36 요청) — 서브PM이 만든 사용가능량 바처럼, 주간 한도 중 "지금까지 써도 되는 권고량"을 표시해 달라는 요청. 메인봇이 스펙을 잡고 코덱스 워커 2개로 병렬 구현 중(파일 분리라 충돌 없음).
2. **와처 busy 오탐** (오후 7:00 발견·수정) — 워커 1bav34가 검수 대기로 홀드 중인데, 보고문에 인용한 "esc to interrupt" 문구 때문에 와처가 화면 전체 텍스트 매칭으로 busy 판정 → "busy인데 1시간째 멈춤!" 오탐 반복. busy 판정을 화면 **하단 상태바 8줄로 한정**하는 수정을 3곳(메인PM 정체·서브PM 정체·중간보고 리마인드)에 일괄 적용 완료. 와처는 15분마다 새로 뜨므로 다음 틱부터 자동 적용.
3. **1bav34 검수 대기** — 작업 완료 후 홀드 중이며 **컨텍스트 100% 소진**. 검수 통과/종료 결정은 오너 몫.

---

## 2. 업무 정의

### 업무 1 — 주간 쿼터 권고 페이스 게이지: 검수 및 확정 【P0 · 오늘】

**배경**: 주간 쿼터 바만으로는 "이 시점에 이만큼 쓴 게 과한지"를 알 수 없음. 경과시간 대비 권고 페이스를 함께 보여줘 소진 속도를 즉시 판단하게 함.

**확정된 스펙 (메인봇 제안, 오너 승인 완료)**
- 권고 페이스 = 주간 윈도우 경과시간 비율 (예: 7일 중 3.5일 경과 → 권고 50%)
- 텍스트 게이지 바에 `|` 마커로 권고 위치 표시
- 바 아래 줄에 `권고 페이스 50% | 잔여권고 12.0%` 형식 표기, 페이스 초과 시 `초과 +X.X%p` 표기
- 적용 범위: **Claude 주간 + Codex 주간** 둘 다, **정기리포트(cbot-quota) + ⚡소모알림(quota-alert)** 양쪽

**남은 절차 (봇이 완료 알림 주면 오너가 진행)**
1. 워커 2개 diff 검수 — 두 파일이 분리되어 있는지, 계산식(경과시간/윈도우 길이)이 리셋 시각 기준인지 확인
2. 라이브 테스트 — 실제 쿼터 값으로 리포트/알림 각 1회 강제 발송
3. 샘플 출력 확인 후 확정

**수용 기준 (DoD)**
- [ ] Claude 주간·Codex 주간 바 양쪽에 `|` 권고 마커가 정확한 위치에 표시
- [ ] 권고 페이스·잔여권고 수치가 리셋 시각(7/15 23:00, 7/18 15:02) 기준으로 맞음
- [ ] 페이스 초과 상황에서 `초과 +X.X%p`가 뜨는 것 확인 (현재 Codex 5시간 79%가 초과 케이스로 쓸 만함 — 단 5시간 바에도 적용했다면)
- [ ] 정기리포트와 소모알림 양쪽 모두 반영

**리스크**: 리셋 시각 경계(윈도우 롤오버 직후 권고 0% 부근)에서 0 나누기·음수 표시 없는지 엣지 확인.

### 업무 2 — 와처 오탐 수정 검증 【P1 · 오늘 저녁】

**수정 내용**: busy 판정 텍스트 매칭을 화면 하단 상태바 8줄로 한정. 동일 버그 3곳(메인PM 정체·서브PM 정체·중간보고 리마인드) 일괄 수리. 실제 1bav34 화면으로 구 로직 오탐 → 신 로직 정상 검증 완료.

**오너 할 일**
- [ ] 다음 와처 틱(15분 주기) 2~3회 동안 1bav34 홀드 화면에 대한 오탐 알림이 재발하지 않는지 관찰
- [ ] 역방향 확인: 진짜 정체(실제 busy 상태로 멈춘 워커)는 여전히 잡아내는지 — 하단 8줄 한정으로 감지 누락이 생기지 않는지 1회 확인

### 업무 3 — SubPM 1bav34 검수·종료 결정 【P0 · 즉시, 오너 결정 필요】

컨텍스트 100% 소진 상태로 홀드 중 — 세션이 쿼터/비용만 점유하고 있으므로 빨리 결정할수록 이득.

- [ ] 1bav34의 최종 보고문 검토 → **검수 통과 처리 또는 종료** 결정
- [ ] 후속 작업이 남았으면 새 세션으로 재기동 (컨텍스트 승계 필요 여부 판단)

### 업무 4 — 포트폴리오 사이트 확정대기 해소 【P1 · 마감 오늘】

태스크보드상 P1 콘텐츠 팩·P2 디자인 시스템이 "완료·확정대기" 상태로 오너 확정을 기다리는 중이며, P3 사이트 구현이 그 뒤에 대기.

- [ ] P1·P2 산출물 확인 후 확정 → P3 착수 언블록 (담당 SubPM j20iyr)

### 업무 5 — 예정 일정 (백로그, 대시보드 헤더 기준)

| # | 항목 | 일정/상태 |
|---|------|-----------|
| #2 | DB분리 audit | 내일(7/13) 10:30 |
| #3 | 뉴스백필 | FM 대기 |
| #5 | Watch 매핑 | 네 확인 대기(오너 확인 필요) |

---

## 3. 오늘 체크리스트 (실행 순서)

1. **[즉시]** 1bav34 보고문 검토 → 검수 통과/종료 결정 (업무 3)
2. **[즉시]** 포트폴리오 P1·P2 확정 → P3 언블록 (업무 4)
3. **[알림 오면]** 쿼터 게이지 diff 검수 → 라이브 테스트 → 샘플 확인 → 확정 (업무 1)
4. **[저녁]** 와처 오탐 재발 여부 + 진짜 정체 감지 여부 관찰 (업무 2)
5. **[내일 10:30]** DB분리 audit (업무 5-#2)

---

## 4. 추가 제안 (선택)

- **비용 페이스도 동일 게이지 적용**: 오늘 하루 $1,085는 작지 않음. 쿼터 게이지와 같은 "경과시간 대비 권고" 로직을 일일/주간 비용에도 붙이면 소진 이상 징후를 같은 문법으로 읽을 수 있음.
- **와처 회귀 케이스 고정**: 이번 오탐의 원인 화면(보고문에 상태 문구 인용)을 와처 테스트 픽스처로 저장해, 이후 와처 로직 수정 때 재발 방지.
- **컨텍스트 소진 조기 경보**: 1bav34처럼 100% 도달 후에야 인지하지 말고, 워커 컨텍스트 80~90% 시점에 와처가 선제 알림을 주도록 룰 추가.
